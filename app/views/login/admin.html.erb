<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Inicio de Sesión - Facturación Electrónica</title>
    </head>
    <body>
        <div class="login-container">
            <h2>Administrador</h2>
            <p>Por favor, ingresa tus credenciales</p>

            <% if flash[:alert] %>
                <p style="color: red;"><%= flash[:alert] %></p>
            <% end %>

            <%= form_with url: login_admin_path, method: :post do |form| %>
            <div class="form-group">
                <%= form.text_field :username, placeholder: "Usuario", required: true, class: "input-color" %>
            </div>

            <div class="form-group">
                <%= form.password_field :password, placeholder: "Contraseña", required: true, class: "input-color" %>
            </div>

            <div class="form-group">
                <button type="submit" class="btn btn-primary">Iniciar Sesión</button>
            </div>

            <% end %>

            <p><%= link_to "Volver a la página de inicio", root_path %></p>
        </div>
    </body>
</html>