<!DOCTYPE html>
<html lang="es">
  <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Panel del Cliente</title>
  </head>
  <body>
    <div class="admin-container">
      <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
      <h1>Bienvenido <%= Cliente.find(session[:cliente_id]).Nombre if session[:cliente_id] %></h1>

      <p>Por favor, selecciona la operacion a realizar</p>
          
      <form action="/cliente/factura" method="get">
        <button type="submit" class="btn admin-btn-primary">Realizar compras</button>
      </form>

      <%= form_with url: logout_path, method: :delete, local: true do |f| %>
        <%= f.submit "Cerrar Sesión", class: "btn-logout" %>
      <% end %>
    </div>
  </body>
</html>